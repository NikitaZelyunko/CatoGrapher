import { FileModelDirective } from './file-model.directive';

describe('FileModelDirective', () => {
  it('should create an instance', () => {
    const directive = new FileModelDirective();
    expect(directive).toBeTruthy();
  });
});
